using System;
using Aspose.Pdf;
using Aspose.Pdf.Facades;
using Aspose.Pdf.Devices;

// Minimal entry point required for the test project to build.
// The actual logic is exercised in the unit tests; this file only
// needs to contain valid C# so the compiler can produce an assembly.
namespace AsposePdfApi
{
    public class Program
    {
        public static void Main(string[] args)
        {
            // No runtime work is needed for the unit‑test‑only project.
            // Keeping the method empty ensures the build succeeds while
            // still providing a valid entry point for the console
            // application template.
        }
    }
}
